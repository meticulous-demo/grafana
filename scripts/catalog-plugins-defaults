# Catalog plugin IDs to bundle under data/plugins-bundled (one per line).
# Optional pin: id:version.
elasticsearch
prometheus
tempo
zipkin
opentsdb
stackdriver
mssql
jaeger
loki
mysql
influxdb
grafana-postgresql-datasource
grafana-pyroscope-datasource
# Pinned so base and PR Meticulous runs screenshot an identical plugin build.
grafana-assistant-app:2.0.51
